[package]
name = "chip8-native-gui"
version = "0.1.0"
edition = "2024"

[dependencies]
bytemuck = { version = "1", features = ["derive"] }
chip8-engine = { path = "../chip8-engine" }
pollster = "0.3"
wgpu = "0.20"
winit = "0.29"
