{
  "name": "chip8-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build:wasm": "sh ./scripts/build-wasm.sh",
    "dev": "npm run build:wasm && vite",
    "build": "npm run build:wasm && tsc -b && vite build",
    "test": "vitest run",
    "check": "npm run build:wasm && tsc -b"
  },
  "dependencies": { "@vitejs/plugin-react": "latest", "vite": "latest", "react": "latest", "react-dom": "latest" },
  "devDependencies": { "@types/react": "latest", "@types/react-dom": "latest", "typescript": "latest", "vitest": "latest" }
}
